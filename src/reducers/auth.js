


const initialState = {
    email: "",
    loggedIn: false,
    provider: ""
}

export default function auth(initialState, action) {
    switch (action.type) {

        default: return initialState
    }
}
